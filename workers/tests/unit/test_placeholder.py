"""Placeholder test — replace with real unit tests.

Unit tests should:
- Test domain models and ports in isolation
- Use no I/O (no DB, no Redis, no HTTP)
- Mock all external dependencies via unittest.mock or pytest-mock
"""


def test_placeholder() -> None:
    """Placeholder that keeps CI green until real tests are added."""
    assert True
