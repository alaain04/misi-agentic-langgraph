# Frontend Code Conventions

- Use ESLint, Prettier, and Husky for linting/formatting. See `eslint.config.js` and `.prettierrc`.
- Follow idiomatic React patterns: function components, hooks, and colocated styles.
- Place shared assets in `src/assets/`.
- Main entry: `src/App.tsx`.
